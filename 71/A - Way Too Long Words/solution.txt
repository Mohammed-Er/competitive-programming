t = int(input())
 
 
for _ in range(t):
    word = input()
    first = word[0]
    last = word[-1]
    if len(word) <= 10:
            print(word)
    else:
        abb = len(word) - 2
        print(f"{first}{abb}{last}")
    
    
 
 